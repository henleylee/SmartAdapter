<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liyunlong.smartadapter.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListViewActivity"
            android:label="@string/listview_single_multi" />
        <activity
            android:name=".RecycleViewActivity"
            android:label="@string/recycleview_single_multi" />
        <activity
            android:name=".HeaderAndFooterWrapperActivity"
            android:label="@string/recycleview_header_footer" />
        <activity
            android:name=".EmptyViewWrapperActivity"
            android:label="@string/recycleview_emptyview" />
    </application>

</manifest>